{
  "private": true,
  "scripts": {
    "build": "yarn run clean && lerna run build --ignore figma-ui",
    "clean": "rimraf '*.log' 'packages/*/{*.log,build,manifest.json}'",
    "fix": "concurrently --raw 'yarn run fix-css' 'yarn run fix-js'",
    "fix-css": "prettier --write 'packages/*/src/**/*.scss'",
    "fix-js": "prettier-standard '*.js' 'packages/*/src/**/*.js'",
    "lint": "concurrently --raw 'yarn run lint-css' 'yarn run lint-js'",
    "lint-css": "stylelint 'packages/*/src/**/*.scss'",
    "lint-js": "standard '*.js' 'packages/*/src/**/*.js'",
    "postinstall": "yarn run build && yarn run typings",
    "publish": "lerna publish from-package",
    "reset": "yarn run clean && lerna clean --yes && rimraf figma.d.ts node_modules 'packages/*/node_modules' yarn.lock && yarn install",
    "test": "ava",
    "typings": "curl --remote-name --silent https://www.figma.com/plugin-docs/figma.d.ts",
    "watch": "yarn run clean && lerna run watch --ignore figma-ui"
  },
  "devDependencies": {
    "@babel/core": "^7.6.2",
    "@babel/plugin-transform-react-jsx": "^7.3.0",
    "@create-figma-plugin/build": "*",
    "ava": "^2.4.0",
    "babel-loader": "^8.0.6",
    "concurrently": "^4.1.2",
    "css-loader": "^3.2.0",
    "esm": "^3.2.25",
    "husky": "^3.0.8",
    "lerna": "^3.16.4",
    "lint-staged": "^9.4.1",
    "node-sass": "^4.12.0",
    "prettier": "^1.18.2",
    "prettier-standard": "^15.0.1",
    "rimraf": "^3.0.0",
    "sass-loader": "^8.0.0",
    "standard": "^14.3.1",
    "style-loader": "^1.0.0",
    "stylelint": "^11.0.0",
    "stylelint-config-prettier": "^6.0.0",
    "stylelint-prettier": "^1.1.1"
  },
  "ava": {
    "files": [
      "packages/*/test/**/*.js",
      "!**/fixtures/**"
    ],
    "verbose": true,
    "require": [
      "esm"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": [
      "standard",
      "prettier-standard",
      "git add"
    ],
    "*.scss": [
      "stylelint",
      "prettier",
      "git add"
    ]
  },
  "stylelint": {
    "extends": [
      "stylelint-prettier/recommended"
    ]
  },
  "workspaces": [
    "packages/*"
  ]
}
