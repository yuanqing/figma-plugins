import { IconSpaceHorizontal16 } from '@create-figma-plugin/ui'
import { h } from 'preact'

import { uiFactory } from '../utilities/ui-factory.js'

export default uiFactory({
  direction: 'left',
  icon: <IconSpaceHorizontal16 />
})
