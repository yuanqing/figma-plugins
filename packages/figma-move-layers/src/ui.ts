import { render } from '@create-figma-plugin/ui'

import { MoveNodes } from './components/move-nodes'
import type { MoveNodesProps } from './utilities/types'

export default render<MoveNodesProps>(MoveNodes)
